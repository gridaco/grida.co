import React from "react";
import Button from "@web/mdx/button";
import SectionLayout from "@web/layouts/section";
export const _MDX_COMPONENTS = {
  h1: props => <h1 style={{ color: "black" }} {...props} />,
};

const Mdx = {
  Button,
  SectionLayout,
};

export default Mdx;
